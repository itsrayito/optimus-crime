import React from 'react';

// this is the page that will be displayed if there is no page to load
const NoMatch = () => {
    return (
        <div>
            Yikes! There seems to be a problem!
        </div>
    );
};

export default NoMatch;